{
  "name": "phonetic-atlas",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "tsc --noEmit && vitest run"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "typebox": "1.1.38",
    "typescript": "^5.9.3",
    "vite": "^5.4.21",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2"
  }
}
